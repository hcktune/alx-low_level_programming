#include <stdio.h>

/**
* main - prints all argumetns per line
* @argc: number of arguments
* @argv: command line arguments
* Return: int
*/

int main(int argc, char *argv[])
{
	int c_arg; /* current_argument */

	for (c_arg = 0; in < argc; c_arg++)
	{
		printf("%s\n", argv[c_arg]);
	}
	return (0);
}
